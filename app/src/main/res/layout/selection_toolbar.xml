<android.support.v7.widget.Toolbar xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/toolbar_height"
    android:background="@drawable/toolbar_selected_background">

    <TextView
        android:id="@+id/selection_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center_vertical|left"
        android:textColor="@color/black"
        android:textSize="@dimen/selected_toolbar_text_size" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end|center_vertical"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/forward_button"
            android:layout_width="@dimen/selected_toolbar_icon_size"
            android:layout_height="@dimen/selected_toolbar_icon_size"
            android:padding="@dimen/padding_12dp"
            android:src="@drawable/ic_ab_fwd_forward" />

        <ImageView
            android:id="@+id/delete_button"
            android:layout_width="@dimen/selected_toolbar_icon_size"
            android:layout_height="@dimen/selected_toolbar_icon_size"
            android:padding="@dimen/padding_12dp"
            android:src="@drawable/ic_ab_fwd_delete" />

    </LinearLayout>

</android.support.v7.widget.Toolbar>